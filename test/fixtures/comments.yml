# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  user: one
  commentable: one (TrackVersion)

two:
  user: two
  commentable: one (TrackVersion)

project_comment:
  user: one
  commentable: one (Project)
